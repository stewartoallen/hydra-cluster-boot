#!/bin/sh

{% include "proc-stub" %}

export zookeeper=$(wait_nodes zookeeper)
export rabbit=$(wait_nodes rabbit)

register_node qmaster
startProcess qmaster ${HYDRA_CMD} {{qmaster_opt}} ${HYDRA_EXE} mqmaster etc web jar
